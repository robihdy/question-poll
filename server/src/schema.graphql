type Query {
  info: String!
  questions: [Question!]!
  question(id: ID!): Question
}

type Mutation {
  ask(description: String!): Question!
  updateQuestion(id: ID!, description: String): Question
  deleteQuestion(id: ID!): Question
}

type Question {
  id: ID!
  description: String!
  votes: Int!
}
